recorder:
  auto_purge: true
  auto_repack: true
  purge_keep_days: 30
  commit_interval: 0
  exclude:
    domains:
      - calendar
      - camera
      - input_select
      - media_player
      - number
      - persistent_notification
      - remote
      - select
      - sun
      - update
