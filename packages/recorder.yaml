recorder:
  auto_purge: true
  auto_repack: true
  purge_keep_days: 90
  commit_interval: 0
  exclude:
    domains:
      - calendar
      - media_player
      - number
      - persistent_notification
      - select
      - sun
      - update
